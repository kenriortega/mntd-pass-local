'use strict'

const API_BASE_ROUTE = 'http://localhost:8080'

export const API_ROUTES = {
  AUTH: `${API_BASE_ROUTE}/auth`,
  SECRETS: `${API_BASE_ROUTE}/secrets`,
  GET_SECRET: `${API_BASE_ROUTE}/secrets`
}
